---
platform: linux
inputs:
- name: playground
outputs:
- name: updated-playground
image_resource:
  type: docker-image
  source:
    repository: pivotalrabbitmq/ci
    email: {{rabbitmq-team-email}}
    username: {{rabbitmq-docker-username}}
    password: {{rabbitmq-docker-password}}
run:
  path: playground/ci/test.sh
